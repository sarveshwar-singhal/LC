#include<iostream>
#include<vector>
using namespace std;

class Solution {
public:
    vector<int> findBall(vector<vector<int>>& grid) {
        vector<int> ans(grid.size(), -1);
        int i, j;
        for(j=0;j<grid[0].size();j++){
            if(j==0 and grid[j][0])
        }
    }
};

int main(){
    return 0;
}